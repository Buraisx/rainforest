<% @products.each do |product|%>
	<div>
		<p><%= product.name %></p>
		<p><%= product.description %></p>
		<p>$<%= product.price %>0</p>
		<%= link_to image_tag(product.url), product_path(product)%>
	</div>
<% end %>
